- `/help` - display this message
- `/add [name]` - create a space (assigned to admin by default)
- `/list [optional limit]` - lists `limit` most recent applications
- `/info [id]` - displays information for the given application
- `/reject [id] [optional message]` - reject an application with an optional message to the applicant.
- `/approve [id] [optional message]` - approve an application with an optional message to the applicant.
- `/assign [id] [pubkey]` - sets the relay's `pubkey` field to the given pubkey making them the owner.
- `/admin add [id] [pubkey]` - adds a pubkey to the relay's admin list.
- `/admin remove [id] [pubkey]` - removes a pubkey from the relay's admin list.
- `/groups enable [id]` - enables NIP 29 groups for the given relay.
- `/groups disable [id]` - disables NIP 29 groups for the given relay.
- `/delete [id]` - deletes the given application and deactivates the corresponding relay.
